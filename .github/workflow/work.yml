name: My Workflow
on:[push]
jobs:
  example-job:
    runs-en: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Say Hello
        run: echo "Hello, Github Actions"
